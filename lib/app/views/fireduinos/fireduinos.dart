import 'package:flutter/material.dart';

class FireduinosPage extends StatelessWidget {
  const FireduinosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Fireduinos page"),
    );
  }
}