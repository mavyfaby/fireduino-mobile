import 'package:flutter/material.dart';

class AccessLogsView extends StatelessWidget {
  const AccessLogsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Access Logs"),
    );
  }
}