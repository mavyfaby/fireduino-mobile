import 'package:flutter/material.dart';

class FireDepartmentsView extends StatelessWidget {
  const FireDepartmentsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Fire Departments"),
    );
  }
}